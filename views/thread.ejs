<!DOCTYPE html>
<html lang="tr">

<head>
    <meta charset="UTF-8">
    <title>Tweet Thread</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-gray-100 text-gray-900 flex h-screen">

    <!-- Sidebar -->
    <aside class="w-64 bg-gray-800 text-white flex flex-col">
        <div class="p-4 text-2xl font-bold border-b border-gray-700">
            📌 Link Collector
        </div>
        <nav class="flex-1 p-4 space-y-2">
            <a href="/" class="block px-3 py-2 rounded hover:bg-gray-700">📋 Linkler</a>
        </nav>
        <div class="p-4 border-t border-gray-700 text-sm text-gray-400">
            by Emirhan & Förbır
        </div>
    </aside>

    <!-- Main -->
    <div class="flex-1 flex flex-col">
        <header class="bg-white shadow p-4 flex justify-between items-center">
            <h1 class="text-xl font-bold">💬 Tweet Thread</h1>
            <a href="/" class="bg-gray-600 text-white px-4 py-2 rounded">← Geri</a>
        </header>

        <main class="flex-1 overflow-y-auto p-6 space-y-4">
            <% if (main) { %>
                <div class="bg-white shadow rounded-lg p-4 border-l-4 border-blue-500">
                    <p class="font-semibold">
                        <%= main.authorName %> <span class="text-gray-500">
                                <%= main.authorHandle %>
                            </span>
                    </p>
                    <p class="mt-2">
                        <%= main.text %>
                    </p>
                    <a href="<%= main.link %>" target="_blank" class="text-blue-600 text-sm">Orijinal Tweet</a>
                    <div class="text-xs text-gray-400 mt-1">
                        <%= main.time %>
                    </div>
                </div>
                <% } %>

                    <% if (replies && replies.length> 0) { %>
                        <% replies.forEach(r=> { %>
                            <div class="bg-white shadow rounded-lg p-4">
                                <p class="font-semibold">
                                    <%= r.authorName %> <span class="text-gray-500">
                                            <%= r.authorHandle %>
                                        </span>
                                </p>
                                <p class="mt-2">
                                    <%= r.text %>
                                </p>
                                <a href="<%= r.link %>" target="_blank" class="text-blue-600 text-sm">Tweet Linki</a>
                                <div class="text-xs text-gray-400 mt-1">
                                    <%= r.time %>
                                </div>
                            </div>
                            <% }) %>
                                <% } else { %>
                                    <p class="text-gray-500">Yanıt bulunamadı.</p>
                                    <% } %>
        </main>
    </div>
</body>

</html>