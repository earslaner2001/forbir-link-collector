<!DOCTYPE html>
<html lang="tr">

<head>
  <meta charset="UTF-8">
  <title>Login</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <style>
    .fade-in-up {
      animation: fadeInUp 0.5s ease-out;
    }

    @keyframes fadeInUp {
      from {
        opacity: 0;
        transform: translateY(20px);
      }

      to {
        opacity: 1;
        transform: translateY(0);
      }
    }
  </style>
</head>

<body class="bg-gray-100 flex items-center justify-center min-h-screen">
  <div class="bg-white p-8 rounded-2xl shadow-xl w-full max-w-sm fade-in-up">
    <h1 class="text-3xl font-bold text-center text-gray-800 mb-6">🔑 Giriş Yap</h1>
    <form method="POST" action="/login" class="flex flex-col gap-4">
      <input type="text" name="username" placeholder="Kullanıcı Adı"
        class="border border-gray-300 p-3 rounded-lg focus:ring-2 focus:ring-blue-500 transition-all duration-200"
        required>
      <input type="password" name="password" placeholder="Şifre"
        class="border border-gray-300 p-3 rounded-lg focus:ring-2 focus:ring-blue-500 transition-all duration-200"
        required>
      <button type="submit"
        class="bg-blue-600 text-white font-semibold p-3 rounded-lg shadow-lg hover:bg-blue-700 transition-all duration-200 transform hover:scale-105">
        Giriş Yap
      </button>
    </form>
    <div class="mt-6 text-center text-gray-500">
      Hesabın yok mu?
      <a href="/register" class="text-blue-600 hover:underline font-semibold">Kayıt ol</a>
    </div>
  </div>
</body>

</html>