<!DOCTYPE html>
<html lang="tr">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Bot Panel</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        @keyframes fadeIn {
            from {
                opacity: 0;
            }

            to {
                opacity: 1;
            }
        }

        .fade-in {
            animation: fadeIn 0.5s ease-out;
        }

        @keyframes slideInLeft {
            from {
                transform: translateX(-100%);
            }

            to {
                transform: translateX(0);
            }
        }

        @keyframes slideOutLeft {
            from {
                transform: translateX(0);
            }

            to {
                transform: translateX(-100%);
            }
        }

        .sidebar-enter-active {
            animation: slideInLeft 0.3s ease-out;
        }

        .sidebar-leave-active {
            animation: slideOutLeft 0.3s ease-out forwards;
        }

        .card-container {
            display: grid;
            gap: 1.5rem;
            grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
        }
    </style>
</head>

<body class="bg-gray-50 text-gray-800 flex flex-col h-screen lg:flex-row">

    <div class="p-4 lg:hidden">
        <button id="menu-toggle" class="text-gray-600 focus:outline-none">
            <svg class="w-8 h-8" fill="none" stroke="currentColor" viewBox="0 0 24 24"
                xmlns="http://www.w3.org/2000/svg">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16">
                </path>
            </svg>
        </button>
    </div>

    <aside id="sidebar"
        class="transform -translate-x-full fixed inset-y-0 left-0 w-64 bg-gray-900 text-gray-200 p-4 space-y-4 flex flex-col z-50 transition-transform duration-300 ease-in-out lg:relative lg:translate-x-0 lg:flex-shrink-0 lg:h-auto">
        <div class="flex justify-between items-center lg:border-b lg:border-gray-700 lg:pb-4">
            <div class="text-2xl font-extrabold text-blue-400 flex items-center gap-2">
                <svg xmlns="http://www.w3.org/2000/svg" class="h-8 w-8 text-purple-400" viewBox="0 0 20 20"
                    fill="currentColor">
                    <path
                        d="M5 3a2 2 0 00-2 2v2a2 2 0 002 2h2a2 2 0 002-2V5a2 2 0 00-2-2H5zM5 11a2 2 0 00-2 2v2a2 2 0 002 2h2a2 2 0 002-2v-2a2 2 0 00-2-2H5zM11 5a2 2 0 012-2h2a2 2 0 012 2v2a2 2 0 01-2 2h-2a2 2 0 01-2-2V5zM11 13a2 2 0 012-2h2a2 2 0 012 2v2a2 2 0 01-2 2h-2a2 2 0 01-2-2v-2z" />
                </svg>
                Link Collector
            </div>
            <button id="close-menu" class="text-gray-500 lg:hidden">
                <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24"
                    xmlns="http://www.w3.org/2000/svg">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12">
                    </path>
                </svg>
            </button>
        </div>
        <nav class="flex-1 space-y-2 text-sm font-medium">
            <a href="/"
                class="flex items-center gap-3 px-4 py-3 rounded-lg transition-all duration-200 hover:bg-gray-800 hover:text-white">
                <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" viewBox="0 0 20 20" fill="currentColor">
                    <path fill-rule="evenodd"
                        d="M4 4a2 2 0 012-2h8a2 2 0 012 2v12a1 1 0 110 2h-3a1 1 0 01-1-1v-4a1 1 0 00-1-1H9a1 1 0 00-1 1v4a1 1 0 01-1 1H4a1 1 0 110-2V4zm3 8a1 1 0 101-1h2a1 1 0 100 2H8a1 1 0 00-1-1z"
                        clip-rule="evenodd" />
                </svg>
                <span>Linkler</span>
            </a>
            <% if (user && user.role==="admin" ) { %>
                <a href="/admin"
                    class="flex items-center gap-3 px-4 py-3 rounded-lg transition-all duration-200 hover:bg-purple-600 hover:text-white">
                    <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" viewBox="0 0 20 20" fill="currentColor">
                        <path fill-rule="evenodd"
                            d="M11.49 3.17c-.38-1.56-2.6-1.56-2.98 0a1.532 1.532 0 01-2.286.948c-1.31-.88-2.86-.38-3.41.948-.55 1.32-.2 2.8.92 3.56.342.23.689.449 1.054.636.556.28.672.988.384 1.547l-.92 1.84a1.151 1.151 0 001.144 1.455l2.09-.348a1.151 1.151 0 011.329.742l.622 1.84a1.151 1.151 0 002.162 0l.621-1.841a1.151 1.151 0 011.33-.742l2.088.348a1.151 1.151 0 001.144-1.455l-.92-1.84a1.151 1.151 0 01.384-1.547c1.12-.76 1.47-2.24.92-3.56-.55-1.32-2.1-1.82-3.41-.948a1.532 1.532 0 01-2.286-.948z"
                            clip-rule="evenodd" />
                    </svg>
                    <span>Admin Paneli</span>
                </a>
                <a href="/bot-panel"
                    class="flex items-center gap-3 px-4 py-3 rounded-lg bg-gray-800 transition-all duration-200 text-white">
                    <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" viewBox="0 0 20 20" fill="currentColor">
                        <path
                            d="M10 2a6 6 0 00-6 6v3.586l-.707.707A1 1 0 004 14h12a1 1 0 00.707-1.707L16 11.586V8a6 6 0 00-6-6zM12 15.5H8a1 1 0 010-2h4a1 1 0 010 2z" />
                    </svg>
                    <span>Bot Paneli</span>
                </a>
                <form action="/logout" method="GET" class="w-full">
                    <button
                        class="w-full text-left flex items-center gap-3 px-4 py-3 rounded-lg transition-all duration-200 hover:bg-red-600 hover:text-white">
                        <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" viewBox="0 0 20 20" fill="currentColor">
                            <path fill-rule="evenodd"
                                d="M3 3a1 1 0 00-1 1v12a1 1 0 102 0V4a1 1 0 00-1-1zm10.293 9.293a1 1 0 001.414 1.414l3-3a1 1 0 000-1.414l-3-3a1 1 0 10-1.414 1.414L14.586 9H7a1 1 0 100 2h7.586l-1.293 1.293z"
                                clip-rule="evenodd" />
                        </svg>
                        <span>Logout (<%= user.username %>)</span>
                    </button>
                </form>
                <% } %>
        </nav>
        <div class="p-4 border-t border-gray-700 text-sm text-gray-400">
            by Förbır Developer Team
        </div>
    </aside>

    <div class="flex-1 flex flex-col p-4 md:p-8 overflow-y-auto">
        <header
            class="bg-white rounded-xl shadow-lg p-6 mb-6 flex justify-between items-center transition-all duration-300">
            <h1 class="text-2xl font-bold text-gray-800">🤖 Bot Panel</h1>
            <a href="/"
                class="bg-gray-600 text-white px-4 py-2 rounded-lg font-semibold hover:bg-gray-700 transition duration-200">←
                Ana Sayfaya Dön</a>
        </header>

        <main class="flex-1">
            <div class="card-container">
                <div class="bg-white p-6 rounded-xl shadow-lg transform hover:scale-105 transition-all duration-300">
                    <h2 class="text-xl font-semibold mb-4 text-gray-800">✉️ Mesaj Gönder</h2>
                    <form method="POST" action="/bot-send" class="flex flex-col gap-3">
                        <input type="text" name="channelId" placeholder="Kanal ID"
                            class="border border-gray-300 p-3 rounded-lg focus:outline-none focus:ring-2 focus:ring-purple-500 transition-all duration-200">
                        <textarea name="message" placeholder="Mesaj..."
                            class="border border-gray-300 p-3 rounded-lg focus:outline-none focus:ring-2 focus:ring-purple-500 transition-all duration-200 h-24 resize-none"></textarea>
                        <button type="submit"
                            class="bg-purple-600 text-white p-3 rounded-lg font-semibold hover:bg-purple-700 transition duration-200">Gönder</button>
                    </form>
                </div>

                <div class="bg-white p-6 rounded-xl shadow-lg transform hover:scale-105 transition-all duration-300">
                    <h2 class="text-xl font-semibold mb-4 text-gray-800">📊 Bot Durumu</h2>
                    <p class="text-gray-700">Sunucu sayısı: <span class="font-bold text-indigo-600">
                            <%= stats.guilds %>
                        </span></p>
                    <p class="text-gray-700">Kullanıcı sayısı: <span class="font-bold text-indigo-600">
                            <%= stats.users %>
                        </span></p>
                </div>

                <div class="bg-white p-6 rounded-xl shadow-lg transform hover:scale-105 transition-all duration-300">
                    <div class="flex items-center justify-between mb-4">
                        <h2 class="text-xl font-semibold text-gray-800">📝 Son Loglar</h2>
                        <form method="POST" action="/reset-logs">
                            <button class="bg-red-500 text-white px-3 py-1 rounded hover:bg-red-600 transition">
                                🔄 Logları Sıfırla
                            </button>
                        </form>
                    </div>

                    <% if (logs && logs.length> 0) { %>
                        <ul class="list-disc pl-5 text-gray-700 space-y-2">
                            <% logs.forEach(log=> { %>
                                <li class="bg-gray-50 p-2 rounded-lg border border-gray-200">
                                    <%= log %>
                                </li>
                                <% }) %>
                        </ul>
                        <% } else { %>
                            <p class="text-gray-500">Henüz log bulunamadı.</p>
                            <% } %>
                </div>

                <div class="bg-white p-6 rounded-xl shadow-lg mt-6">
                    <h2 class="text-xl font-semibold mb-4 text-gray-800">⏱️ Rate Limit Logları</h2>
                    <ul id="rateLimitLogs" class="space-y-2 text-sm text-gray-700"></ul>
                </div>

                <script>
                    socket.on("rateLimitLog", (log) => {
                        const list = document.getElementById("rateLimitLogs");
                        if (!list) return;
                        const li = document.createElement("li");
                        li.className = "bg-red-50 border border-red-200 p-2 rounded";
                        li.textContent = `[${log.time}] ${log.user} cooldown’a takıldı (${log.remaining}sn kaldı)`;
                        list.prepend(li);
                        if (list.children.length > 20) list.removeChild(list.lastChild);
                    });
                </script>

                <div class="bg-white p-6 rounded-xl shadow-lg transform hover:scale-105 transition-all duration-300">
                    <h2 class="text-xl font-semibold mb-4 text-gray-800">⚡ Hızlı Aksiyonlar</h2>
                    <div class="flex flex-col gap-3">
                        <form method="POST" action="/reset">
                            <button
                                class="w-full bg-red-600 text-white px-4 py-3 rounded-lg font-semibold hover:bg-red-700 transition duration-200">
                                🗑️ Tümünü Sıfırla
                            </button>
                        </form>
                        <form method="POST" action="/bot-send">
                            <input type="hidden" name="channelId" value="<%= process.env.CHANNEL_ID ||
'' %>">
                            <input type="hidden" name="message"
                                value="📢 Yayın başladı! https://kick.com/forbir1 @everyone">
                            <button
                                class="w-full bg-green-600 text-white px-4 py-3 rounded-lg font-semibold hover:bg-green-700 transition duration-200">
                                📢 Yayın Başladı
                            </button>
                        </form>
                    </div>
                </div>

                <div class="bg-white p-6 rounded-xl shadow-lg transform hover:scale-105 transition-all duration-300">
                    <h2 class="text-xl font-semibold mb-4 text-gray-800">🎮 Durum Mesajı</h2>
                    <form method="POST" action="/set-status" class="flex flex-col md:flex-row gap-3">
                        <input type="text" name="status" placeholder="Yeni durum (örn: Kick’te canlı yayında)"
                            class="border border-gray-300 p-3 rounded-lg flex-1 focus:outline-none focus:ring-2 focus:ring-blue-500 transition-all duration-200">
                        <button
                            class="bg-blue-600 text-white px-6 py-3 rounded-lg font-semibold hover:bg-blue-700 transition duration-200">Güncelle
                        </button>
                    </form>
                </div>
            </div>
        </main>
    </div>

    <script>
        const menuToggle = document.getElementById('menu-toggle');
        const closeMenu = document.getElementById('close-menu');
        const sidebar = document.getElementById('sidebar');

        menuToggle.addEventListener('click', () => {
            sidebar.classList.remove('-translate-x-full');
            sidebar.classList.add('sidebar-enter-active');
            sidebar.classList.remove('sidebar-leave-active');
        });

        closeMenu.addEventListener('click', () => {
            sidebar.classList.add('sidebar-leave-active');
            sidebar.classList.remove('sidebar-enter-active');
            setTimeout(() => {
                sidebar.classList.add('-translate-x-full');
            }, 300);
        });
    </script>
</body>

</html>