<!DOCTYPE html>
<html lang="tr">

<head>
    <meta charset="UTF-8">
    <title>RSS Tweetler</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        .fade-in {
            animation: fadeIn 0.5s ease-out;
        }

        @keyframes fadeIn {
            from {
                opacity: 0;
            }

            to {
                opacity: 1;
            }
        }
    </style>
</head>

<body class="bg-gray-50 text-gray-800 flex flex-col min-h-screen">
    <header class="bg-white shadow-sm p-4 flex justify-between items-center z-10">
        <h1 class="text-2xl font-bold text-gray-700">🐦 RSS’ten Çekilen Tweetler</h1>
        <a href="/"
            class="bg-gray-600 text-white font-semibold px-6 py-2 rounded-full shadow-lg hover:bg-gray-700 transition-all duration-200 transform hover:scale-105">
            ← Geri
        </a>
    </header>

    <main class="flex-1 overflow-y-auto p-6 bg-gray-100">
        <div class="mb-6 p-4 bg-white rounded-xl shadow-md fade-in">
            <p class="text-gray-700"><strong>Kaynak:</strong>
                <span class="font-mono text-sm text-blue-600">
                    <%= rssUrl %>
                </span>
            </p>
        </div>

        <% if (tweets && tweets.length> 0) { %>
            <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
                <% tweets.forEach(t=> { %>
                    <div
                        class="bg-white rounded-2xl shadow-lg p-6 relative transition-transform duration-300 hover:scale-105 hover:shadow-2xl fade-in">
                        <p class="font-semibold mb-2 leading-snug">
                            <%= t.title %>
                        </p>
                        <a href="<%= t.link %>" target="_blank"
                            class="text-blue-600 font-medium hover:underline transition-colors duration-200">Tweet
                            Linki</a>
                        <div class="text-xs text-gray-400 mt-2">
                            <span class="flex items-center gap-1">
                                <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4" viewBox="0 0 20 20"
                                    fill="currentColor">
                                    <path fill-rule="evenodd"
                                        d="M10 18a8 8 0 100-16 8 8 0 000 16zm1-12a1 1 0 10-2 0v4a1 1 0 00.293.707l2.828 2.829a1 1 0 101.415-1.415L11 9.586V6z"
                                        clip-rule="evenodd" />
                                </svg>
                                <%= t.date %>
                            </span>
                        </div>
                    </div>
                    <% }) %>
            </div>
            <% } else { %>
                <p class="text-gray-500 text-center text-lg mt-12">Tweet bulunamadı.</p>
                <% } %>
    </main>
</body>

</html>